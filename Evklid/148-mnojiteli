#include <iostream>
#include <math.h>
//не проходят все тесты - неоптимальный расход памяти
//главная функция
int main ()
{
  int a;
  std::cin >> a;
  for (int i=2; i <= a/2; i++)
    {
      if (a%i==0)
      {
        std::cout << i << " ";
      }
    }
  return 0;
}
