#include <iostream>
#include <math.h>
//главная функция
int main ()
{
  int a;
  std::cin >> a;
  //Выведет множители числа кроме 1 и самого себя
  //Если нужна 1 то пишем в цикле от 1, если нужно само число то пишем до i<=a
  //for (int i=1; i <= a; i++)
  for (int i=2; i < a; i++)
    {
      if (a%i==0)
      {
        std::cout << i << " ";
      }
    }
  return 0;
}
